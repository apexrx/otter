[package]
name = "otter"
version = "0.5.0"
edition = "2024"

[dependencies]
jsonschema = "0.45.0"
regex = "1.12.3"
serde_json = "1.0.149"

[dev-dependencies]
serde = { version = "1.0.228", features = ["derive"] }
